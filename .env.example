# Server-side only. Read by the jev-proxy container; never reaches the app bundle.
TYPESAFE_API_KEY=

# Baked into the app bundle at build time, so it must be a public URL.
# Local dev: http://localhost:8788/systemone
VITE_JEV_PROXY_URL=https://example.com/systemone
